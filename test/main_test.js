const {expect} = require('chai');
//test suite
describe('Mocha is go!', () => {
    //test spec (unit test)
    it('should run our tests using npm', () => {
        expect(true).to.be.ok;
    });
});
